# This package is a shared test suite and should only be used in test contexts.
name: kiss_queue_tests
description: A test suite for kiss_queue
version: 0.2.0
repository: https://github.com/WAMF/kiss_queue_tests

environment:
  sdk: ^3.8.0

dev_dependencies:
  collection: ^1.18.0
  test: ^1.24.0
  kiss_queue: ^0.2.1
  very_good_analysis: ^9.0.0

topics:
  - testing
